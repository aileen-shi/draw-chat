import React from "react";
import { useNavigate } from "react-router-dom";

const ChatLog = () => {
    const navigate = useNavigate();

    return <div className="message__container"></div>;
};
export default ChatLog;
